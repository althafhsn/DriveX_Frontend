import { RentedCarFilterPipe } from './rented-car-filter.pipe';

describe('RentedCarFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RentedCarFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
